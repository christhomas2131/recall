# Claude Code guide

Read and follow [AGENTS.md](AGENTS.md).

For a fresh clone, run `npm ci` and `npm run doctor`, then report which runtime is available. When Claude Code is authenticated, launch `npm run app:claude` so in-app work uses the local Claude Code session. Otherwise use `npm start` for automatic selection. Do not request or print secrets.
